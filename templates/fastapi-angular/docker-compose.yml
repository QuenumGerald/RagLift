services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    env_file: .env
    volumes:
      - chroma-data:/app/.raglift/chroma
      - ./backend/docs:/app/docs
  frontend:
    build: ./frontend
    ports: ["4200:4200"]
    depends_on: [backend]
volumes:
  chroma-data:
