# RagLift VS Code Extension Scaffold

![RagLift Logo](../../RagLift_Logo.png)

This is a minimal VS Code extension scaffold that can consume the RagLift package.

## What it does

- Registers a `RagLift: Ask` command
- Provides a starting point for wiring the CLI or the package API

## Next step

- Replace the stub command with a call into `raglift`
- Add commands for ingesting docs and displaying sources
