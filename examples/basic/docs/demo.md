# RagLift demo

RagLift is a lightweight Python SDK and CLI for inspectable RAG pipelines.
It ingests local Markdown, text, and PDF files, chunks them, stores embeddings in Chroma, and returns answers with citations.

The basic example uses fake providers so it can run without an OpenAI API key.
